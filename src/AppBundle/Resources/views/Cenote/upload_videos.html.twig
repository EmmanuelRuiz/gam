{% extends "AppBundle:Layouts:layout.html.twig" %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Subir vídeo del registro</h1>
            </div>
        </div>

        {%for message in app.session.flashbag().get('msg')%}
            <div class="alert alert-success">{{message}}</div>
        {%endfor%}
    </div>

    <div class="col-lg-4 box-form">
        <h2>{{cenote.name}}</h2>

        {{form_start(form, {'enctype':'multipart/form-data'})}}
        {{form_end(form)}}
    </div>
{% endblock%}